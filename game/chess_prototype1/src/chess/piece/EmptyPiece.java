package chess.piece;

import chess.utils.ColorType;
import chess.utils.PieceType;

public class EmptyPiece extends Piece {

	public EmptyPiece(int x, int y, ColorType color, PieceType type) {
		super(x, y, color, type);
	}
}
